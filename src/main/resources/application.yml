server:
  port: 8888 # Config Server port

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hasratparwez9162/config-repo.git
          default-label: main # branch of the repository
          searchPaths:
            - '/'